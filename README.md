# Toybox

A collection of simple Go and Ebitengine experiments with WebAssembly support.

## WebAssembly Static Site Generator

To build WebAssembly binaries for all games and generate the static website for GitHub Pages:

```bash
cd web
go run .
```

The output will be generated in `dist/`.
